{
  "name": "getskilled-backend",
  "version": "1.0.0",
  "description": "Backend for GetSkilled platform",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "npm audit fix --force && npm install --production",
    "postinstall": "npm audit fix --force"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^7.0.3",
    "nodemailer": "^7.0.6",
    "twilio": "^5.6.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
